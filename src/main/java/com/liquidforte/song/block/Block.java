package com.liquidforte.song.block;

import com.liquidforte.song.tile.AbstractTile;

import java.awt.*;

public class Block extends AbstractTile {
    public Block(byte tile, Color color, boolean solid) {
        super(tile, color, solid);
    }
}
