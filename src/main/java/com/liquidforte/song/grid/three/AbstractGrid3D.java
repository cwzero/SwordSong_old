package com.liquidforte.song.grid.three;

/*
import com.liquidforte.song.grid.AbstractGrid;
import com.liquidforte.song.math.geometry.three.Point3D;
import com.liquidforte.song.math.geometry.three.Size3D;

public abstract class AbstractGrid3D<V> extends AbstractGrid<Point3D, Size3D, V> implements Grid3D<V> {
    @Override
    protected V doGet(Object key) {
        if (key instanceof Point3D p) {
            return getValue(p);
        }
        return null;
    }

    @Override
    public V remove(Object key) {
        if (key instanceof Point3D p) {
            return removeKey(p);
        }
        return null;
    }

    @Override
    public boolean containsKey(Object key) {
        if (key instanceof Point3D p) {
            return contains(p);
        }
        return false;
    }
}
*/